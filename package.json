{
  "name": "ai-hub-console",
  "version": "0.2.0",
  "private": true,
  "description": "Local-first AI usage dashboard and sound alert console.",
  "scripts": {
    "dev": "node server/server.js",
    "start": "node server/server.js",
    "check": "node --check server/server.js && node --check server/connectors/openai-compatible.js && node --check web/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
